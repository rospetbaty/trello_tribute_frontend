import React from 'react';
import ReactDOM from 'react-dom';
// import TodoBox from './index.jsx';

let data = [
  {
    title: "Test"
  }
];

document.addEventListener('DOMContentLoaded', function() {
  ReactDOM.render(
    <h1>"hello"</h1>,
    document.getElementById('container')
  );
}, false);
